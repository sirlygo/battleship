# Render Blueprint: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: battleship
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
